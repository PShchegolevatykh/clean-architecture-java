services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=jdbc:h2:mem:flashcardsdb
      - DATABASE_USER=sa
      - DATABASE_PASSWORD=password
      - PORT=8080
